//! Check if is prime

use num_bigint::BigUint;

pub fn is_prime(n: BigUint) -> bool {
    /*if n <= 1 {
        return false;
    }
    for a in 2..n {
        if n % a == 0 {
            return false;
        }
    }
    true
    */

    false
}